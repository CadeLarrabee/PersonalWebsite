// import { createContext, useState } from "react";
// import PropTypes from "prop-types";

// const LastRemovedDiskContext = createContext();

// export const LastRemovedDiskProvider = ({ children }) => {
//   const [lastRemovedDisk, setLastRemovedDisk] = useState(null);

//   return (
//     <LastRemovedDiskContext.Provider
//       value={{ lastRemovedDisk, setLastRemovedDisk }}
//     >
//       {children}
//     </LastRemovedDiskContext.Provider>
//   );
// };

// LastRemovedDiskProvider.propTypes = {
//   children: PropTypes.node.isRequired,
// };

// export default LastRemovedDiskContext;
